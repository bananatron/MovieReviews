<div class="row">
  <div class="large-2 columns">
    <center><%= link_to image_tag("http://image.tmdb.org/t/p/w130"+@review.movie.image), @review.movie, class:"th" if @review.movie.image %><br>
    <strong><%= link_to @review.movie.name, @movie %></strong>
    </center>
  </div>
  <div class="large-10 columns">
    <div class="row">
      <div class="large-10 columns">
        <% if @movie.moviedb_id == nil %>
        <div class="panel callout radius">
           We can't get much information about this movie. Can you <%= link_to 'help identify it?', confirm_movie_path(@movie) %></a>
        </div>
        <% end %>

      <%= render 'summary', :review => @review %>
      
      </div>
    
      <div align="center" class="large-2 columns">
        
        <%= render '/reviews/score', :review => @review %>
        
      </div>
    </div>
  
  
  
<p><a href="#" data-reveal-id="firstModal"><img src="http://png-2.findicons.com/files/icons/2564/max_mini_icon/16/flag_red.png"></a>
<!-- Reveal Modals begin -->
<div id="firstModal" class="reveal-modal" data-reveal>
  <h2>Flag this summary?</h2>
  <p>Summaries which are flagged are reviewed by a moderator for invalid or inappropriate data.</p>
  <p>Are you sure you want to flag this summary?</p>
  <p><%= button_to "Yes I'm sure", flag_review_path, class:"secondary radius button", method:"get" %>
  <a class="close-reveal-modal">&#215;</a>
</div>

  
  
  </div>
</div>

