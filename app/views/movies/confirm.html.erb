<div class="panel callout radius"><i>Current movie name: <strong><%= @movie.name %></strong></i></div><br>

<h3>We found some movies this summary might belong to:</h3><br><br>
<% params %>

<div class="row">
<% if @results.count > 0 %>
  <% @results.each do |r|%>
    <% if r.popularity > 0.3 %>
    <div class="large-9 columns">
      <div class="panel">
        <h4><%= link_to "+ " + r.title, confirm_dbid_movie_path(moviedb_id: r.id) %> <i>(<%= r.release_date[0...4] %>)</i></h4> <!--<%= r.id%>-->
        
        <% movie_details ||= Tmdb::Movie.detail(r.id) %>
        <p><%= movie_details.overview %>
      </div>
    </div>  

    <div class="large-3 columns">
      <% if r.poster_path %>
      <img src="http://image.tmdb.org/t/p/w130<%= r.poster_path%>"></br></br></br>
      <% else %>
      <img src="http://placehold.it/80x80&text=!">
      <% end %>    
    </div> 
    <% end %>
  <% end %>
<% else %>
  </div>
<p>Just kidding, we couldn't find any. Flag this below for review.</p>

<% end %>



<p> Is the right movie not on the list? <br>
  <!-- Flag info -->
    <p><a href="#" data-reveal-id="firstModal">Flag this movie for review by a moderator.</p></a>
    <!-- Reveal Modals begin -->
    <div id="firstModal" class="reveal-modal" data-reveal>
      <h2>Flag this movie?</h2>
      <p>Movies which are flagged are reviewed by a moderator for invalid or inappropriate data.</p>
      <p>Are you sure you want to flag <%= @movie.name %>?</p>
      <p><%= button_to "Yes I'm sure", flag_movie_path, class:"secondary radius button", method:"get" %>
      <a class="close-reveal-modal">&#215;</a>
  
